import { ExpandedDetailDirective } from './expanded-detail.directive';

describe('ExpandedDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandedDetailDirective();
    expect(directive).toBeTruthy();
  });
});
